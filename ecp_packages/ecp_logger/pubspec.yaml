name: ecp_logger
description: A new flutter plugin project.
version: 0.0.1
homepage: http://www.toshibatec-eu.com/
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  plugin:
    platforms:
      android:
        package: com.tgcs.ecp_logger
        pluginClass: EcpLoggerPlugin
