part of 'theme.dart';

// coverage:ignore-file
class ECPButtonStyles {
  ECPButtonStyles._();

  static ButtonStyle primaryButtonStyle = ElevatedButton.styleFrom(
      primary: UiuxColours.primaryButtonColour, elevation: 0.0);

  static ButtonStyle secondaryButtonStyle = ElevatedButton.styleFrom(
      primary: UiuxColours.secondaryButtonColour, elevation: 0.0);
}
